---
// importing block stylesheet
import Button from "../button.astro";
import "./styles.css"

interface Props {
    Eyebrow?: string;
    hasEyebrow?: boolean;
    marketingHeading: string;
    lede?: string;
    noLede?: boolean;
    blockName?: string;
    situation?: "hero" | "content-section";
    theme?: "dark" | "light";
    alignment?: "left" | "center" | "right";
    HasButtons?: boolean;
    buttonOneText?: string;
    buttonOneTitle?: string;
    buttonOneHref?: string;
    hasSecondBtn?: boolean;
    buttonTwoText?: string;
    buttonTwoTitle?: string;
    buttonTwoHref?: string;
}

const {
    alignment = "center",
    theme = "dark",
    Eyebrow,
    hasEyebrow = true,
    marketingHeading,
    lede,
    blockName,
    situation = "content-section",
    HasButtons = false,
    buttonOneText,
    buttonOneTitle,
    buttonOneHref,
    hasSecondBtn = false,
    buttonTwoText,
    buttonTwoTitle,
    buttonTwoHref,
    noLede = false
} = Astro.props;

const headingMapping = {
    hero: "h1",
    "content-section": "h2",
} as const;

const Tag = headingMapping[situation];
const MarketingHeadingTag = hasEyebrow ? "p" : Tag;
---
<article class={blockName && `${blockName}__intro`} data-intro-alignment={alignment} data-intro-theme={theme}>
    {hasEyebrow && (
        <Tag class={blockName && `${blockName}-intro__eyebrow`}>{Eyebrow}</Tag>
    )}
    <MarketingHeadingTag class={blockName && `${blockName}-intro__marketing-heading`} data-size={hasEyebrow ? situation : undefined}>{marketingHeading}</MarketingHeadingTag>
    {!noLede && (
        <p class={blockName && `${blockName}-intro__lede`}>{lede}</p>
    )}
    {HasButtons && (
        <div class={blockName && `${blockName}-intro__buttons`}>
          <Button text={buttonOneText} href={buttonOneHref} theme="primary" title={buttonOneTitle}/>
          {hasSecondBtn && <Button text={buttonTwoText} href={buttonTwoHref} theme="secondary" title={buttonTwoTitle}/>}
        </div>
    )}
</article>